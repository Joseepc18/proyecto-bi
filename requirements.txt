# Dependencias del proyecto. Se van agregando a medida que avanza el pipeline.

# Peticiones HTTP a las APIs (Remotive, Jooble) y futuros scrapers
requests==2.34.2

# Leer variables sensibles (API keys) desde el archivo .env
python-dotenv==1.2.2

# Parsear el HTML de los portales de empleo (scraping)
beautifulsoup4==4.15.0

# Procesamiento y homologación de datos en el pipeline ETL
pandas==2.3.1

# Conector de PostgreSQL para cargar el Data Warehouse (Entregable 4)
psycopg2-binary==2.9.10